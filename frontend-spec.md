# Plusらぼ サイトリニューアル フロントエンド仕様書

**作成日:** 2026-05-08
**プロジェクト:** Plusらぼ（草津市）サイトリニューアル
**ドキュメント種別:** 実装前の仕様確認レポート

---

## 1. プロジェクト概要

草津市のPlusらぼのコーポレートサイトリニューアル。デザインカンプは完成済みで、本ドキュメントは実装着手前のフロントエンド仕様確認のために作成した取り決め一覧。

- **デザインカンプ配置場所:** `/lab-renewal/desing/` 配下のPDFファイル
  - `top｜デザイン_02.pdf` / `top｜デザイン_02_hover.pdf`（トップ）
  - `plusらぼについて｜デザイン.pdf` / `sp-plusらぼについて｜デザイン.pdf`
  - `事業者向けサービス｜デザイン.pdf` / `sp-事業者向けサービス｜デザイン.pdf`
  - `就労支援サービス｜デザイン.pdf` / `sp-就労支援サービス｜デザイン.pdf`
  - `iPhone 13 & 14 - 4.pdf`
- **保守体制:** 自社で保守管理。一部下層ページはWordPressで構築（WP部分はクライアント側で対応）

---

## 2. 対応環境

| 項目 | 仕様 |
|------|------|
| 対応ブラウザ | Chrome / Safari / Edge / Firefox の各最新版（バージョン指定なし） |
| モバイルブラウザ | iOS Safari / Android Chrome（バージョン指定なし） |
| IE11 | **非対応** |
| 1920px超のワイドモニター | デザインカンプの最大幅指示に従う |
| 390px未満（iPhone SE 1st 等） | 一応閲覧できるよう対応 |

---

## 3. ブレイクポイント・スケーリング

### ブレイクポイント

- **PC:** 769px以上
- **SP:** 768px以下
- **タブレット中間レイアウトなし**（768px〜390pxは同一レイアウト）

### スケーリング方針（vwベース等比拡縮）

| 画面幅 | 基準 | 挙動 |
|--------|------|------|
| 1729px以上 | 1728px時のサイズで固定 | フォント・余白とも拡大しない |
| 769px〜1728px | 1728pxを100%として `vw` で等比拡縮 | 画面幅に比例して縮小 |
| 391px〜768px | 390pxを100%として `vw` で等比拡縮 | 画面幅に比例して拡縮 |
| 390px以下 | 390px時のサイズ準拠 | はみ出さないよう調整 |

### 実装方法

- 数値は基本的に `vw` または `clamp()` で記述
- 必要に応じて `px` 等の絶対値も使用可
- フォントサイズも上記ルールに従う

---

## 4. 技術スタック

| 項目 | 仕様 |
|------|------|
| HTML | HTML5（`.html` 形式で直書き運用） |
| CSS | ピュアCSS（プリプロセッサ不使用） |
| JavaScript | バニラJS + 指定ライブラリ |
| ビルド環境 | **なし**（VS Codeで直接編集） |
| npm | **不使用**（CDN読み込み） |
| バージョン管理 | クライアント側でGitHubにアップロード |

---

## 5. CSS設計

### 設計手法

- **BEM** で命名
- **ピュアCSS** で記述（SCSS等のプリプロセッサ不使用）
- モダンCSS機能を活用：
  - ネスト記法（`&` セレクタ）
  - `:is()` `:has()` `:where()` 等の擬似クラス
  - `:root` でカスタムプロパティ管理（カラー・フォント・余白等）

### 共通スタイルの管理

- ボタン・カラー・タイポグラフィ等の共通要素は `common.css`（または `_components.css`）でBEM形式で定義
- 例：`.c-button` `.c-button--primary` `.c-button--secondary`

---

## 6. JavaScript ライブラリ

すべてCDN（公式CDNのバージョン固定URL）から `<script>` タグで読み込み。

| ライブラリ | 用途 | 備考 |
|-----------|------|------|
| **GSAP** | スクロール連動アニメーション | 無料版のみ。`SplitText` `ScrollSmoother` 等の有料プラグインは不使用 |
| **Swiper** | スライダー実装 | - |
| **Lenis** | 慣性スクロール | アンカーリンクは Lenis の `scrollTo` メソッドで制御 |

---

## 7. フォント

### メインフォント

- **LINE Seed JP**
- 入手元：[LINE Seed 公式サイト](https://seed.line.me/)
- 配置：`/fonts/` ディレクトリに自前ホスト
- 読込：`@font-face` で読込

### 使用ウェイト

- **Regular（400）**
- **Bold（700）**
- ※ Thin / ExtraBold は読み込まない（表示速度最適化）

### サブフォント（漢字フォールバック）

- **Noto Sans JP**
- LINE Seed JP の収録外文字を補完

### font-family の指定例

```css
:root {
  --font-base: "LINE Seed JP", "Noto Sans JP", sans-serif;
}
```

---

## 8. 共通パーツの管理方法

### マークアップ管理

- **方式：HTML直書き（コピペ運用）**
- ヘッダー・フッターは各ページに直接記述
- 修正発生時は全ページの該当箇所を手動で更新する運用

### 共通スタイル

- ヘッダー・フッター・ボタンの**スタイル**は `common.css` で一元管理
- HTMLマークアップ自体はCSSで管理できないため、上記マークアップは各ページに記述

### 静的ページとWPページのデザイン統一

- クライアント側で対応

---

## 9. ディレクトリ構成

フラット構成で運用。

```
/
├── index.html
├── about.html
├── service-business.html
├── service-work.html
├── ...
├── /css
│   ├── style.css
│   ├── common.css
│   └── ...
├── /js
│   ├── main.js
│   └── ...
├── /images
│   ├── (jpg, png, svg)
│   └── ...
└── /fonts
    └── (LINE Seed JP, Noto Sans JP)
```

### コード規約

- インデント・改行コード等：**VS Code標準設定に準拠**
- 画像ファイル命名規則：実装側で決定（kebab-case 推奨）

---

## 10. デザイン素材

| 項目 | 内容 |
|------|------|
| カンプ形式 | PDF |
| カンプ配置 | `/lab-renewal/desing/` 配下 |
| 画像書き出し | クライアント支給 |
| 画像フォーマット | JPG / PNG（クライアント支給） |
| SVGアイコン | 使用あり |

---

## 11. アニメーション・インタラクション

| 項目 | 仕様 |
|------|------|
| スクロール連動アニメーション | あり（GSAP使用） |
| ホバー時の挙動 | デザインカンプ（`top｜デザイン_02_hover.pdf` 等）に従う |
| ページ遷移時のローディング/トランジション演出 | なし |
| 動画・GIFの使用 | なし |
| 自動再生 | なし |

---

## 12. SEO・解析・公開準備

| 項目 | 仕様 |
|------|------|
| メタ情報（title / description） | claude code側で作成 |
| OGP画像・ファビコン | クライアントから支給予定（用意でき次第連絡） |
| 構造化データ（JSON-LD） | 不要 |
| Google Analytics / Search Console / GTM | クライアント側で設置 |
| sitemap.xml / robots.txt | 取り決めなし |
| 多言語対応 | なし |
| 404 / メンテナンスページ | 不要 |

---

## 13. アクセシビリティ・パフォーマンス

| 項目 | 仕様 |
|------|------|
| アクセシビリティ目標 | 特に設定なし（一般的な配慮レベルで実装） |
| パフォーマンス目標値 | 特に設定なし |
| 画像最適化 | クライアント支給のJPG/PNGをそのまま使用 |

---

## 14. WordPress構築範囲

クライアント側で対応するため、フロントエンド側では以下の点のみ留意：

- 共通パーツ（ヘッダー・フッター・ボタン）はWP側でも同じデザインで再現される前提
- `common.css` のクラス設計はWP側でも流用しやすいよう、汎用性を意識した命名にする

WordPressに関する以下事項はクライアント側で実施：
- 該当ページの選定
- テーマ構築（既存 or オリジナル）
- 投稿タイプ・カスタムフィールド設定
- URL設計
- サーバー環境構築（PHPバージョン、DB、SSL）

---

## 15. フォーム・機能要件

クライアント側で対応するため、フロントエンド側では未実装。

- お問い合わせフォーム
- フォーム送信先・文面
- Googleマップ埋め込み
- SNS連携（Instagram埋め込み等）

---

## 16. 納品・確認フロー

| 項目 | 内容 |
|------|------|
| 希望納期 | 指定なし |
| 確認用環境 | ローカル（zip共有等） |
| 中間チェック | **未確定**（要相談） |
| ソース管理 | ファイル一式を納品 → クライアント側でGitHubにアップロード |

---

## 17. 実装上の注意点

### キャッシュ対策

- CSS / JS 更新時はファイル名にバージョン文字列を付与
- 例：`<link rel="stylesheet" href="/css/style.css?v=20260508">`

### iOS Safari の100vh問題

- ファーストビュー等で `100vh` を使う箇所は `100dvh` を併用
- 例：`height: 100vh; height: 100dvh;`

### Lenis + アンカーリンク

- 慣性スクロール中のページ内リンクは、Lenis の `scrollTo` メソッドで制御
- 例：`lenis.scrollTo("#section-id")`

### 1920px超の挙動

- カンプの最大幅指示に従う
- フォント・余白は1728pxで固定

---

## 18. 残タスク・未確定事項

実装着手前に確定が必要な事項：

| 項目 | 状態 |
|------|------|
| 中間チェックのタイミング | **要相談**（トップページ完成時 / ページごと / 全完成後 のいずれか） |
| OGP画像・ファビコン | クライアント側で用意でき次第支給 |
| メタ情報（title / description） | クライアント側から支給待ち |

---

## 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-05-08 | 初版作成（仕様確認ディスカッションの結果を反映） |
